-- _Basic is designed to be everything in the prelude that is horribly tied into the compiler or
-- cyclically dependent or such. In essence it's the bit needing hi bootstrap hackery. 
-- apart from this we shouldn't need any hi hacking ...
#ifndef __HADDOCK__
module YHC._Basic where
#endif

